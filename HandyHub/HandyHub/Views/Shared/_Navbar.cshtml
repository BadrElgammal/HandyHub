<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top shadow-sm">
    <div class="container-fluid px-4">

        <a class="navbar-brand fw-bold fs-3" href="@Url.Action("Index", "Home")">
            <i class="fas fa-toolbox text-warning me-2"></i> HandyHub
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav ms-auto align-items-lg-center">

                <li class="nav-item"><a class="nav-link" href="/">الرئيسية</a></li>
                <li class="nav-item"><a class="nav-link" href="@Url.Action("SearchWorkers", "Client")">ابحث عن عاملين</a></li>

                @if (User.Identity.IsAuthenticated)
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" data-bs-toggle="dropdown">
                            <img src="~/images/default-avatar.png" class="rounded-circle me-2" width="30" height="30">
                            <span>@User.Identity.Name</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end text-end">
                            <li><a class="dropdown-item" href="@Url.Action("Profile", "Account")"><i class="fas fa-user me-2"></i> الملف الشخصي</a></li>
                            @if (User.IsInRole("Client"))
                            {
                                <li><a class="dropdown-item" href="@Url.Action("Dashboard", "Client")"><i class="fas fa-chart-line me-2"></i> لوحة العميل</a></li>
                            }
                            @if (User.IsInRole("Worker"))
                            {
                                <li><a class="dropdown-item" href="@Url.Action("Dashboard", "Worker")"><i class="fas fa-toolbox me-2"></i> لوحة العامل</a></li>
                            }
                            @if (User.IsInRole("Admin"))
                            {
                                <li><a class="dropdown-item text-danger" href="@Url.Action("Dashboard", "Admin")"><i class="fas fa-crown me-2"></i> لوحة المدير</a></li>
                            }
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item text-danger" href="@Url.Action("Logout", "Account")"><i class="fas fa-sign-out-alt me-2"></i> خروج</a></li>
                        </ul>
                    </li>
                }
                else
                {
                    <li class="nav-item ms-2">
                        <a href="@Url.Action("Login", "Account")" class="btn btn-warning btn-sm text-dark fw-bold me-2">دخول</a>
                    </li>
                    <li class="nav-item">
                        <a href="@Url.Action("Register", "Account")" class="btn btn-success btn-sm text-white fw-bold">تسجيل</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>