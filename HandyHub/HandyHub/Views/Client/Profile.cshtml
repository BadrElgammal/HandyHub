@using HandyHub.Models.ViewModels
@model ClientDashboardVM
@model HandyHub.Models.ViewModels.ClientDashboardVM
@{
    ViewData["Title"] = "HandyHub - لوحة العميل";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@Model.Client.Id</h1>
<h1>@Model.Client.User.Name</h1>
<h1>@Model.Client.User.City</h1>
<h1>@Model.Client.User.Email</h1>
<div class="min-h-screen py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

<h1>@Model.Reviews.Count</h1>
<h1>@Model.Favorites.Count</h1>

@foreach(var item in Model.Reviews)
{
	@foreach(var i in Model.Workers)
        <!-- 🧍 بيانات العميل -->
        <div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden mb-8 relative">

            <div class="h-32 bg-gradient-to-br from-teal-600 to-blue-600 w-full"></div>

            <div class="px-6 pb-6">
                <div class="flex flex-col md:flex-row items-center md:items-end -mt-12 gap-6">

                    <!-- Profile Image -->
                    <div class="relative">
                        <img src="@Model.Client.User?.ImageUrl" class="w-32 h-32 rounded-full border-4 border-white shadow-md object-cover" />
                        <div class="absolute bottom-2 right-2 w-5 h-5 bg-green-500 border-2 border-white rounded-full"></div>
                    </div>

                    <!-- Name & Info -->
                    <div class="flex-1  md:text-right mt-2 md:mt-0">
                        <h2 class="text-3xl font-bold text-gray-800">@Model.Client.User.Name</h2>

                        <div class="flex flex-wrap justify-center md:justify-start gap-4 mt-3 text-sm text-gray-600">
                            <span class="flex items-center gap-1"><i class="ri-map-pin-line"></i>@Model.Client.User.City</span>
                            <span class="flex items-center gap-1"><i class="ri-phone-line"></i>@Model.Client.User.Phone</span>
                            <span class="flex items-center gap-1"><i class="ri-mail-line"></i>@Model.Client.User.Email</span>
                        </div>
                    </div>

                    <!-- Buttons -->
                    <div class="flex gap-3 mt-4 md:mt-0">
                        <a asp-action="EditClient" asp-route-id="@Model.Client.Id"
                           class="px-5 py-2.5 bg-white border border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 hover:text-blue-600 transition shadow-sm">
                            <i class="ri-edit-box-line ml-1"></i> تعديل
                        </a>


                        <form asp-action="DeleteWorker" asp-route-id="@Model.Client.Id" method="post" onsubmit="return confirm('هل أنت متأكد من حذف هذا العميل؟');">

                            <button type="submit" class="px-5 py-2.5 bg-red-50 border border-red-100 text-red-600 font-medium rounded-lg hover:bg-red-100 transition shadow-sm">

                                <i class="ri-delete-bin-line ml-1"></i> حذف الحساب
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <!-- 🟨 البطاقات الإحصائية -->
        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8 text-right">
            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-gray-600 text-sm mb-1">إجمالي الحجوزات</p>
                        <p class="text-3xl font-bold text-gray-900">0</p>
                    </div>
                    <div class="w-12 h-12 bg-teal-100 rounded-lg flex items-center justify-center">
                        <i class="ri-calendar-check-line text-teal-600 text-2xl"></i>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-gray-600 text-sm mb-1">المفضلة</p>
                        <p class="text-3xl font-bold text-gray-900">@Model.Favorites.Count()</p>
                    </div>
                    <div class="w-12 h-12 bg-pink-100 rounded-lg flex items-center justify-center">
                        <i class="ri-heart-fill text-pink-600 text-2xl"></i>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-gray-600 text-sm mb-1">التقييمات المقدّمة</p>
                        <p class="text-3xl font-bold text-gray-900">@Model.Reviews.Count()</p>
                    </div>
                    <div class="w-12 h-12 bg-yellow-100 rounded-lg flex items-center justify-center">
                        <i class="ri-star-fill text-yellow-600 text-2xl"></i>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-gray-600 text-sm mb-1">إجمالي الإنفاق</p>
                        <p class="text-3xl font-bold text-gray-900">—</p>
                    </div>
                    <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                        <i class="ri-money-dollar-circle-line text-blue-600 text-2xl"></i>
                    </div>
                </div>
            </div>
        </div>

        <!-- 🔹 Tabs -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
            <div class="border-b border-gray-200 flex">
                <button id="tab-favorites" class="flex-1 px-6 py-4 text-center font-medium cursor-pointer text-teal-600 border-b-2 border-teal-600 bg-teal-50">
                    <i class="ri-heart-line ml-2"></i> العمّال المفضلون
                </button>
                <button id="tab-reviews" class="flex-1 px-6 py-4 text-center font-medium text-gray-600 hover:text-gray-900 hover:bg-gray-50">
                    <i class="ri-star-line ml-2"></i> تقييماتي
                </button>
            </div>

            <div id="tabs-container" class="max-h-[600px] overflow-y-auto p-6">

                <!-- 💖 المفضلون -->
                <div id="content-favorites">
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 text-right">
                        @foreach (var fav in Model.Favorites)
	{
		if (i.Id == @item.WorkerId)
                            var worker = Model.Workers.FirstOrDefault(w => w.Id == fav.WorkerId);
                            if (worker != null)
		{
			<h1>@i.User.Name</h1>
                                <div class="bg-white rounded-lg border border-gray-200 overflow-hidden hover:shadow-lg transition-shadow">
                                    <img alt="@worker.User.Name" class="w-full h-60 object-cover object-top" src="@worker.User.ImageUrl" />
                                    <div class="p-4">
                                        <div class="flex items-center justify-between mb-2">
                                            <h3 class="text-lg font-semibold text-gray-900">@worker.User.Name</h3>
                                            <form method="post" asp-controller="Favorites" asp-action="Remove" asp-route-id="@fav.Id">
                                                <button class="w-8 h-8 flex items-center justify-center text-pink-600 hover:bg-pink-50 rounded-full" title="إزالة">
                                                    <i class="ri-heart-fill text-xl"></i>
                                                </button>
                                            </form>
                                        </div>
                                        <p class="text-gray-600 mb-2">@worker.Category?.Name</p>
                                        <div class="flex items-center justify-between mb-4 text-sm">
                                            <div class="flex items-center space-x-1 space-x-reverse">
                                                <i class="ri-star-fill text-yellow-400"></i>
                                                <span class="font-semibold text-gray-900">@Model.Reviews.Where(r => r.WorkerId == worker.Id && r.ClientId == Model.Client.Id).FirstOrDefault().Rating</span>
                                            </div>
                                            <span class="text-gray-600"><i class="ri-map-pin-line ml-1"></i> @worker.User.City</span>
                                        </div>
                                        <a href="~/Client/WorkerProfile/@worker.Id"
                                           class="block w-full px-4 py-2 bg-teal-600 text-white text-center rounded-lg hover:bg-teal-700 font-medium">
                                            عرض الملف الشخصي
                                        </a>
                                    </div>
                                </div>
		}
	}
	<h1>@item.Rating</h1>
	<h1>@item.Comment</h1>

                        @if (!Model.Favorites.Any())
                        {
                            <p class="text-gray-500">لا توجد عناصر مفضلة حالياً.</p>
}
                    </div>
                </div>

@foreach (var item in Model.Favorites)
                <!-- ⭐ التقييمات -->
                <div id="content-reviews" class="hidden space-y-4">
                    @foreach (var review in Model.Reviews)
{
	@foreach (var i in Model.Workers)
                        var worker = Model.Workers.FirstOrDefault(w => w.Id == review.WorkerId);
                        if (worker != null)
	{
		if (i.Id == @item.WorkerId)
                            <div class="bg-gray-50 rounded-lg p-6 border border-gray-200">
                                <div class="flex items-start space-x-4 space-x-reverse">
                                    <img class="w-16 h-16 rounded-lg object-cover" src="@worker.User.ImageUrl" alt="@worker.User.Name" />
                                    <div class="flex-1">
                                        <div class="flex justify-between mb-2">
                                            <a href="/Worker/Profile/@worker.Id"
                                               class="text-lg font-semibold text-gray-900 hover:text-teal-600">@worker.User.Name</a>
                                            <span class="text-sm text-gray-600">@review.CreatedAt.ToShortDateString()</span>
                                        </div>
                                        <div class="flex items-center mb-3 space-x-1 space-x-reverse">
                                            @for (int i = 0; i < review.Rating; i++)
		{
			<h1>@i.User.Name</h1>
                                                <i class="ri-star-fill text-yellow-400"></i>
                                            }
                                        </div>
                                        <p class="text-gray-700">@review.Comment</p>
                                    </div>
                                </div>
                            </div>
		}
	}
	<h1>@item.CreatedAt</h1>

                    @if (!Model.Reviews.Any())
                    {
                        <p class="text-gray-500">لا توجد تقييمات بعد.</p>
}
                </div>

            </div>
        </div>
        <!-- قسم CTA -->
        <div class="mt-8 bg-gradient-to-r from-teal-600 to-teal-700 rounded-xl p-8 text-white text-right">
            <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
                <div>
                    <h2 class="text-2xl font-bold mb-2">هل تحتاج إلى خدمة؟</h2>
                    <p class="text-teal-100">تصفح العمال المهرة واحجز خدمتك القادمة بسهولة</p>
                </div>
                <a asp-action="search" class="px-6 py-3 bg-white text-teal-600 rounded-lg hover:bg-gray-100 font-medium">
                    <i class="ri-search-line ml-2"></i> ابحث عن عمّال
                </a>
            </div>
        </div>
    </div>
</div>



<a class="btn btn-warning" asp-action="EditClient" asp-route-id="@Model.Client.Id">Edite</a>
<form asp-action="DeleteClient"
	  method="post"
	  onsubmit="return confirm('هل أنت متأكد من حذف هذا العميل؟');"
	  style="display:inline-block;">
	<input type="hidden" name="id" value="@Model.Client.Id" />
	<button type="submit"
			class="btn btn-sm btn-danger rounded-3"
			title="حذف">
		<i class="fas fa-trash"></i>
	</button>
</form>
@section Scripts {
    <script>
        const tabs = {
            favorites: document.getElementById('content-favorites'),
            reviews: document.getElementById('content-reviews')
        };
        const buttons = {
            favorites: document.getElementById('tab-favorites'),
            reviews: document.getElementById('tab-reviews')
        };

        Object.keys(buttons).forEach(key => {
            buttons[key].addEventListener('click', () => {
                Object.keys(tabs).forEach(k => {
                    tabs[k].classList.add('hidden');
                    buttons[k].classList.remove('text-teal-600', 'border-b-2', 'border-teal-600', 'bg-teal-50');
                    buttons[k].classList.add('text-gray-600');
                });
                tabs[key].classList.remove('hidden');
                buttons[key].classList.remove('text-gray-600');
                buttons[key].classList.add('text-teal-600', 'border-b-2', 'border-teal-600', 'bg-teal-50');
            });
        });
    </script>
}